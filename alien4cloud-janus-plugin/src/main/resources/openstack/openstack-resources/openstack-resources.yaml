tosca_definitions_version: tosca_simple_yaml_1_0_0_wd03

template_name: Janus-plugin-openstack-if-types
template_author: Alien 4 Cloud
template_version: 1.0.0

description: "Defines resources for the Janus plugin, openstack configuration."

imports:
  - tosca-normative-types:1.0.0-SNAPSHOT

node_types:
  janus.nodes.openstack.Image:
    abstract: true
    derived_from: tosca.nodes.Root
    properties:
      id:
        type: string
    capabilities:
      os:
        type: tosca.capabilities.OperatingSystem

  janus.nodes.openstack.Flavor:
    abstract: true
    derived_from: tosca.nodes.Root
    properties:
      id:
        type: string
    capabilities:
      host:
        type: tosca.capabilities.Container
        
artefact_types:
  janus.artefacts.openstack.Implementation.Bash:
    description: Mock artefact for sh
    derived_from: tosca.artifacts.Implementation.Bash
  