tosca_definitions_version: tosca_simple_yaml_1_0_0_wd03

template_name: Janus-plugin-slurm-types
template_author: Alien 4 Cloud
template_version: 1.0.0

description: "Defines resources for the Janus plugin, mocking slurm configuration."

imports:
  - tosca-normative-types:1.0.0-ALIEN12

node_types:
  janus.nodes.slurm.Image:
    abstract: true
    derived_from: tosca.nodes.Root
    properties:
      id:
        type: string
    capabilities:
      os:
        type: tosca.capabilities.OperatingSystem

  janus.nodes.slurm.Flavor:
    abstract: true
    derived_from: tosca.nodes.Root
    properties:
      id:
        type: string
    capabilities:
      host:
        type: tosca.capabilities.Container
        
artefact_types:
  janus.artefacts.slurm.Implementation.Bash:
    description: Mock artefact for sh
    derived_from: tosca.artifacts.Implementation.Bash
  